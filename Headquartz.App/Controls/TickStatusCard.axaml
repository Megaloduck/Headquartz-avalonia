<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="using:Headquartz.App.Controls"
             mc:Ignorable="d" d:DesignWidth="240" d:DesignHeight="110"
             x:Class="Headquartz.App.Controls.TickStatusCard"
             x:Name="Root">

	<!--
        Tick bar   : SegmentCount = TicksPerWorkHour (4/5/6/7 by difficulty)
                     FilledCount  = whole completed ticks in current work hour
                     PartialFillRatio = ms elapsed in current tick / TickDelayMs
                     → each pill animates smoothly as ms pass

        Work hours : SegmentCount = 8 (always)
                     FilledCount  = whole work hours completed in current day
                     No partial — snaps on hour completion
    -->

	<Border
        Background="{DynamicResource BrushBgElevated}"
        BorderBrush="{DynamicResource BrushBorderSubtle}"
        BorderThickness="1"
        CornerRadius="{DynamicResource RadiusMd}"
        Padding="12,10">

		<StackPanel Spacing="10">

			<!-- DATE HEADER -->
			<TextBlock
                Text="{Binding DateLabel, ElementName=Root}" 
                Classes ="sidebar-title" />

			<!-- TICK STATUS BAR -->
			<StackPanel Spacing="5">
				<StackPanel Orientation="Horizontal" Spacing="6">
					<Path
                        Data="{DynamicResource IconRefreshCw}"
                        Stroke="{DynamicResource BrushTextMuted}"
                        StrokeThickness="1.5"
                        Fill="Transparent"
                        Width="11" Height="11"
                        Stretch="Uniform"
                        VerticalAlignment="Center"/>
					<TextBlock
                        Text="Tick status" Classes="figure" />
				</StackPanel>

				<controls:SegmentedProgressBar
                    SegmentCount="{Binding TicksPerWorkHour, ElementName=Root}"
                    FilledCount="{Binding TicksElapsedInWorkHour, ElementName=Root}"
                    PartialFillRatio="{Binding TickPartialFillRatio, ElementName=Root}"
                    FilledBrush="{DynamicResource BrushSuccess}"
                    EmptyBrush="{DynamicResource BrushBorderSubtle}"/>
			</StackPanel>

			<!-- WORK HOURS BAR -->
			<StackPanel Spacing="5">
				<StackPanel Orientation="Horizontal" Spacing="6">
					<Path
                        Data="{DynamicResource IconClock}"
                        Stroke="{DynamicResource BrushTextMuted}"
                        StrokeThickness="1.5"
                        Fill="Transparent"
                        Width="11" Height="11"
                        Stretch="Uniform"
                        VerticalAlignment="Center"/>
					<TextBlock
                        Text="Work hours" Classes="figure" />
				</StackPanel>

				<controls:SegmentedProgressBar
                    SegmentCount="8"
                    FilledCount="{Binding WorkHoursElapsedInDay, ElementName=Root}"
                    FilledBrush="{DynamicResource BrushInfo}"
                    EmptyBrush="{DynamicResource BrushBorderSubtle}"/>
			</StackPanel>

		</StackPanel>

	</Border>

</UserControl>