<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Headquartz.App.LogisticsTrackingView">

	<ScrollViewer>
		<StackPanel Margin="30" Spacing="20">

			<!-- HEADER -->
            <StackPanel Spacing="4">
                <TextBlock FontSize="34" FontWeight="Bold" Text="Delivery Tracking"/>
				<TextBlock FontSize="13" Foreground="{DynamicResource BrushTextSecondary}"	 Text="Real-time tracking, delivery ETAs, exception handling, and proof of delivery."/>
            </StackPanel>			

			<!-- OVERDUE BANNER -->
			<Border IsVisible="{Binding Overdue}"
                    Background="{DynamicResource BrushAlertCriticalBg}" CornerRadius="12" Padding="20">
				<StackPanel Orientation="Horizontal" Spacing="12">
					<TextBlock Text="🚚" FontSize="22" VerticalAlignment="Center"/>
					<StackPanel>
						<TextBlock
                            Text="{Binding Overdue, StringFormat='{}{0} SHIPMENT(S) PAST DEADLINE'}"
                            FontSize="16" FontWeight="Bold" Foreground="{DynamicResource BrushBadgeDangerFg}"/>
						<TextBlock Text="These deliveries are damaging reputation every tick."
                            Foreground="{DynamicResource BrushBadgeDangerFg}"/>
					</StackPanel>
				</StackPanel>
			</Border>

			<!-- KPI ROW -->
			<ItemsControl ItemsSource="{Binding Kpis}">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<WrapPanel ItemSpacing="10"/>
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>
				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<Border Width="200" Margin="0,5" Padding="20" Background="{DynamicResource BrushBgCard}" CornerRadius="12">
							<StackPanel>
								<TextBlock FontSize="13" Foreground="{DynamicResource BrushTextSecondary}" Text="{Binding Title}"/>
								<TextBlock FontSize="22" FontWeight="Bold" Text="{Binding Value}"/>
							</StackPanel>
						</Border>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>

			<!-- SEARCH + FILTERS -->
			<Grid ColumnDefinitions="*,16,Auto">

				<TextBox Grid.Column="0"
                    Text="{Binding SearchText}"
                    PlaceholderText="Search by client or product..."
                    Padding="12,9" Background="{DynamicResource BrushBgCard}"
                    BorderBrush="{DynamicResource BrushBorderStrong}" BorderThickness="1" CornerRadius="8"/>

				<ItemsControl Grid.Column="2" ItemsSource="{Binding StatusFilters}">
					<ItemsControl.ItemsPanel>
						<ItemsPanelTemplate>
							<StackPanel Orientation="Horizontal" Spacing="8"/>
						</ItemsPanelTemplate>
					</ItemsControl.ItemsPanel>
					<ItemsControl.ItemTemplate>
						<DataTemplate>
							<Button Padding="14,8" CornerRadius="8" Background="{DynamicResource BrushBgCard}"
                                Command="{Binding $parent[ItemsControl].DataContext.SetFilterCommand}"
                                CommandParameter="{Binding}"
                                Content="{Binding}"/>
						</DataTemplate>
					</ItemsControl.ItemTemplate>
				</ItemsControl>

			</Grid>

			<!-- STATUS -->
			<Border IsVisible="{Binding StatusMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                    Padding="14,10" Background="{DynamicResource BrushBgNested}" CornerRadius="8">
				<TextBlock Text="{Binding StatusMessage}" FontSize="13" TextWrapping="Wrap"/>
			</Border>

			<!-- TRACKING TABLE -->
			<Border Padding="20" Background="{DynamicResource BrushBgCard}" CornerRadius="12">
				<StackPanel>
					<TextBlock FontSize="22" FontWeight="Bold" Text="Active Shipments" Margin="0,0,0,16"/>

					<TextBlock IsVisible="{Binding !Shipments.Count}"
                        Text="No shipments match this filter." Foreground="{DynamicResource BrushTextSecondary}"/>

					<ItemsControl ItemsSource="{Binding Shipments}">
						<ItemsControl.ItemTemplate>
							<DataTemplate>
								<Border Margin="0,5" Padding="16" Background="{DynamicResource BrushBgNested}" CornerRadius="8">
									<StackPanel Spacing="10">

										<!-- Header row -->
										<Grid ColumnDefinitions="*,130,120,Auto">

											<StackPanel Grid.Column="0">
												<StackPanel Orientation="Horizontal" Spacing="8">
													<TextBlock FontWeight="Bold" FontSize="14"
                                                        Text="{Binding ClientName}"
                                                        VerticalAlignment="Center"/>
													<Border IsVisible="{Binding IsOverdue}"
                                                            Padding="6,2" Background="{DynamicResource BrushAlertCriticalBg}"
                                                            CornerRadius="4" VerticalAlignment="Center">
														<TextBlock Text="OVERDUE" FontSize="9"
                                                            FontWeight="Bold" Foreground="{DynamicResource BrushBadgeDangerFg}"/>
													</Border>
												</StackPanel>
												<TextBlock FontSize="12" Foreground="{DynamicResource BrushTextSecondary}"
                                                    Text="{Binding ProductName}"/>
											</StackPanel>

											<StackPanel Grid.Column="1" VerticalAlignment="Center">
												<TextBlock FontSize="11" Foreground="{DynamicResource BrushTextSecondary}"
                                                    Text="Location"/>
												<TextBlock FontSize="12" FontWeight="SemiBold"
                                                    Text="{Binding CurrentLocation}"
                                                    Foreground="{DynamicResource BrushAccentSecondary}"/>
											</StackPanel>

											<StackPanel Grid.Column="2" VerticalAlignment="Center">
												<TextBlock FontSize="11" Foreground="{DynamicResource BrushTextSecondary}"
                                                    Text="Deadline"/>
												<TextBlock FontSize="12" Text="{Binding Deadline}"
                                                    Foreground="{DynamicResource BrushTextSecondary}"/>
											</StackPanel>

											<Button Grid.Column="3"
                                                Padding="12,7" Background="{DynamicResource BrushSuccess}" CornerRadius="6"
                                                VerticalAlignment="Center"
                                                Command="{Binding $parent[ItemsControl].DataContext.MarkDeliveredCommand}"
                                                CommandParameter="{Binding}"
                                                Content="✓ Delivered"/>

										</Grid>

										<!-- Progress bar -->
										<StackPanel Spacing="4">
											<StackPanel Orientation="Horizontal" Spacing="8">
												<ProgressBar
                                                    Value="{Binding ProgressPercent}"
                                                    Maximum="100"
                                                    Height="7" Width="400"
                                                    Foreground="{DynamicResource BrushAccentSecondary}"
                                                    VerticalAlignment="Center"/>
												<TextBlock FontSize="11" Foreground="{DynamicResource BrushTextSecondary}"
                                                    Text="{Binding ProgressPercent, StringFormat='{}{0}%'}"
                                                    VerticalAlignment="Center"/>
											</StackPanel>
											<StackPanel Orientation="Horizontal" Spacing="16">
												<TextBlock FontSize="11" Foreground="{DynamicResource BrushTextSecondary}"
                                                    Text="{Binding Status}"/>
												<TextBlock FontSize="11" Foreground="{DynamicResource BrushTextMuted}"
                                                    Text="{Binding LastUpdated, StringFormat='Updated: {0}'}"/>
											</StackPanel>
										</StackPanel>

									</StackPanel>
								</Border>
							</DataTemplate>
						</ItemsControl.ItemTemplate>
					</ItemsControl>

				</StackPanel>
			</Border>

		</StackPanel>
	</ScrollViewer>
</UserControl>