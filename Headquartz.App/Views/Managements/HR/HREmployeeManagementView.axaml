<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Class="Headquartz.App.HREmployeeManagementView">

	<ScrollViewer>
		<StackPanel Margin="30" Spacing="20">

			<!-- HEADER -->
            <StackPanel Spacing="4">
				<TextBlock FontSize="34" FontWeight="Bold" Text="Employee Management"/>
				<TextBlock FontSize="13" Foreground="{DynamicResource BrushTextSecondary}" Text="Staff directory, search or filter, contract details, employment history and status."/>
			</StackPanel>

			<!-- SUMMARY ROW -->
			<WrapPanel ItemSpacing="10">
				<Border Width="200" Padding="20" Background="{DynamicResource BrushBgCard}" CornerRadius="12">
					<StackPanel>
						<TextBlock FontSize="13" Foreground="{DynamicResource BrushTextSecondary}" Text="Total Employees"/>
						<TextBlock FontSize="28" FontWeight="Bold" Text="{Binding TotalEmployees}"/>
					</StackPanel>
				</Border>
				<Border Width="200"  Padding="20" Background="{DynamicResource BrushBgCard}" CornerRadius="12">
					<StackPanel>
						<TextBlock FontSize="13" Foreground="{DynamicResource BrushTextSecondary}" Text="At-Risk Employees"/>
						<TextBlock FontSize="28" FontWeight="Bold" Text="{Binding AtRiskCount}"/>
					</StackPanel>
				</Border>
				<Border Width="200" Padding="20" Background="{DynamicResource BrushBgCard}" CornerRadius="12">
					<StackPanel>
						<TextBlock FontSize="13" Foreground="{DynamicResource BrushTextSecondary}" Text="Monthly Payroll"/>
						<TextBlock FontSize="28" FontWeight="Bold" Text="{Binding TotalPayroll, StringFormat='${0:N0}'}"/>
					</StackPanel>
				</Border>
			</WrapPanel>

			<!-- FILTER -->
			<StackPanel Orientation="Horizontal" Spacing="12">
				<TextBlock Text="Filter by Department:" VerticalAlignment="Center" Foreground="{DynamicResource BrushTextSecondary}"/>
				<ComboBox
                    ItemsSource="{Binding DepartmentFilters}"
                    SelectedItem="{Binding FilterDepartment}"
                    MinWidth="180"/>
			</StackPanel>

			<!-- EMPLOYEE LIST -->
			<Border Padding="20" Background="{DynamicResource BrushBgCard}" CornerRadius="12">
				<StackPanel>
					<TextBlock FontSize="22" FontWeight="Bold" Text="Employees" Margin="0,0,0,16"/>

					<TextBlock
                        IsVisible="{Binding !Employees.Count}"
                        Text="No employees found."
                        Foreground="{DynamicResource BrushTextSecondary}"/>

					<ItemsControl ItemsSource="{Binding Employees}">
						<ItemsControl.ItemTemplate>
							<DataTemplate>
								<Border
                                    Margin="0,5" Padding="16"
                                    Background="{DynamicResource BrushBgNested}"
                                    CornerRadius="8">
									<StackPanel Spacing="10">

										<!-- Name row -->
										<Grid ColumnDefinitions="*,Auto,Auto">
											<StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10">
												<TextBlock
                                                    FontSize="15"
                                                    FontWeight="Bold"
                                                    Text="{Binding Name}"
                                                    VerticalAlignment="Center"/>
												<Border
                                                    IsVisible="{Binding IsLowMorale}"
                                                    Background="{DynamicResource BrushAlertCriticalBg}"
                                                    CornerRadius="6"
                                                    Padding="6,2"
                                                    VerticalAlignment="Center">
													<TextBlock
                                                        Text="AT RISK"
                                                        FontSize="10"
                                                        FontWeight="Bold"
                                                        Foreground="{DynamicResource BrushBadgeDangerFg}"/>
												</Border>
												<Border
                                                    IsVisible="{Binding IsAssigned}"
                                                    Background="{DynamicResource BrushAlertInfoBg}"
                                                    CornerRadius="6"
                                                    Padding="6,2"
                                                    VerticalAlignment="Center">
													<TextBlock
                                                        Text="ON TASK"
                                                        FontSize="10"
                                                        FontWeight="Bold"
                                                        Foreground="{DynamicResource BrushBadgeInfoFg}"/>
												</Border>
											</StackPanel>

											<!-- Boost morale button -->
											<Button
                                                Grid.Column="1"
                                                Margin="8,0"
                                                Padding="10,6"
                                                FontSize="12"
                                                Background="{DynamicResource BrushBgInput}"
                                                CornerRadius="6"
                                                Command="{Binding $parent[ItemsControl].DataContext.BoostMoraleCommand}"
                                                CommandParameter="{Binding}"
                                                Content="💊 Boost (−$500)"/>

											<!-- Fire button -->
											<Button
                                                Grid.Column="2"
                                                Padding="10,6"
                                                FontSize="12"
                                                Background="{DynamicResource BrushBadgeDangerBg}"
                                                CornerRadius="6"
                                                Command="{Binding $parent[ItemsControl].DataContext.FireEmployeeCommand}"
                                                CommandParameter="{Binding}"
                                                Content="🔥 Fire"/>
										</Grid>

										<!-- Dept + Role + Salary -->
										<StackPanel Orientation="Horizontal" Spacing="20">
											<TextBlock FontSize="12" Foreground="{DynamicResource BrushTextSecondary}"
                                                Text="{Binding Department}"/>
											<TextBlock FontSize="12" Foreground="{DynamicResource BrushTextSecondary}"
                                                Text="{Binding Role}"/>
											<TextBlock FontSize="12" Foreground="{DynamicResource BrushTextSecondary}"
                                                Text="{Binding Salary, StringFormat='${0:N0}/mo'}"/>
										</StackPanel>

										<!-- Morale bar -->
										<StackPanel Orientation="Horizontal" Spacing="8">
											<TextBlock Text="Morale" FontSize="11" Foreground="{DynamicResource BrushTextSecondary}" Width="70"/>
											<ProgressBar
                                                Value="{Binding Morale}"
                                                Maximum="100"
                                                Height="7"
                                                Width="200"
                                                Foreground="{DynamicResource BrushDeptHR}"
                                                VerticalAlignment="Center"/>
											<TextBlock
                                                FontSize="11"
                                                Foreground="{DynamicResource BrushTextSecondary}"
                                                Text="{Binding MoraleStatus}"
                                                VerticalAlignment="Center"/>
										</StackPanel>

										<!-- Productivity bar -->
										<StackPanel Orientation="Horizontal" Spacing="8">
											<TextBlock Text="Productivity" FontSize="11" Foreground="{DynamicResource BrushTextSecondary}" Width="70"/>
											<ProgressBar
                                                Value="{Binding Productivity}"
                                                Maximum="100"
                                                Height="7"
                                                Width="200"
                                                Foreground="{DynamicResource BrushSuccess}"
                                                VerticalAlignment="Center"/>
											<TextBlock
                                                FontSize="11"
                                                Foreground="{DynamicResource BrushTextSecondary}"
                                                Text="{Binding Productivity, StringFormat={}{0}%}"
                                                VerticalAlignment="Center"/>
										</StackPanel>

									</StackPanel>
								</Border>
							</DataTemplate>
						</ItemsControl.ItemTemplate>
					</ItemsControl>
				</StackPanel>
			</Border>

		</StackPanel>
	</ScrollViewer>
</UserControl>