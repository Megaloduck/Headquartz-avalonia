<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Class="Headquartz.App.FinanceBudgetAllocationView">
	
	<ScrollViewer>
		<StackPanel Margin="30" Spacing="20">

			<!-- HEADER -->
            <StackPanel Spacing="4">
				<TextBlock FontSize="34" FontWeight="Bold" Text="Budget Allocation"/>
				<TextBlock FontSize="13" Foreground="{DynamicResource BrushTextSecondary}" Text="Create budgets, allocate funds, track department spending, and forecast adjustments."/>
			</StackPanel>

			<!-- SUMMARY ROW -->
			<WrapPanel ItemSpacing="10	">
				<Border Width="200" Margin="0,5" Padding="20" Background="{DynamicResource BrushBgCard}" CornerRadius="12">
					<StackPanel>
						<TextBlock FontSize="13" Foreground="{DynamicResource BrushTextSecondary}" Text="Company Cash"/>
						<TextBlock FontSize="23" FontWeight="Bold"
                            Text="{Binding CompanyCash, StringFormat='${0:N0}'}"/>
					</StackPanel>
				</Border>
				<Border Width="200" Margin="0,5" Padding="20" Background="{DynamicResource BrushBgCard}" CornerRadius="12">
					<StackPanel>
						<TextBlock FontSize="13" Foreground="{DynamicResource BrushTextSecondary}" Text="Total Budget Allocated"/>
						<TextBlock FontSize="23" FontWeight="Bold"
                            Text="{Binding TotalBudget, StringFormat='${0:N0}'}"/>
					</StackPanel>
				</Border>
				<Border Width="200" Margin="0,5" Padding="20" Background="{DynamicResource BrushBgCard}" CornerRadius="12">
					<StackPanel>
						<TextBlock FontSize="13" Foreground="{DynamicResource BrushTextSecondary}" Text="Operational Cost / Tick"/>
						<TextBlock FontSize="23" FontWeight="Bold"
                            Text="{Binding TotalOperationalCost, StringFormat='${0:N0}'}"/>
					</StackPanel>
				</Border>
			</WrapPanel>

			<!-- ACTIONS -->
			<WrapPanel ItemSpacing="10">
				<Button Padding="14" Margin="0,5" Command="{Binding ResetAllCommand}" Content="↺  Reset All Budgets"/>
			</WrapPanel>

			<!-- DEPARTMENT BUDGET TABLE -->
			<Border Padding="20" Background="{DynamicResource BrushBgCard}" CornerRadius="12">
				<StackPanel>
					<TextBlock FontSize="22" FontWeight="Bold" Text="Department Budgets" Margin="0,0,0,16"/>

					<ItemsControl ItemsSource="{Binding Departments}">
						<ItemsControl.ItemTemplate>
							<DataTemplate>
								<Border Margin="0,6" Padding="16" Background="{DynamicResource BrushBgNested}" CornerRadius="8">
									<Grid ColumnDefinitions="32,*,180,Auto">

										<!-- Emoji -->
										<TextBlock
                                            Grid.Column="0"
                                            Text="{Binding Emoji}"
                                            FontSize="18"
                                            VerticalAlignment="Center"/>

										<!-- Name + health bars -->
										<StackPanel Grid.Column="1" Spacing="6" Margin="8,0">
											<TextBlock
                                                FontSize="14"
                                                FontWeight="SemiBold"
                                                Text="{Binding Name}"/>
											<StackPanel Orientation="Horizontal" Spacing="16">
												<StackPanel Orientation="Horizontal" Spacing="6">
													<TextBlock Text="Eff" FontSize="11" Foreground="{DynamicResource BrushTextSecondary}" Width="22"/>
													<ProgressBar
                                                        Value="{Binding Efficiency}"
                                                        Maximum="100"
                                                        Height="5" Width="80"
                                                        Foreground="{DynamicResource BrushSuccess}"
                                                        VerticalAlignment="Center"/>
													<TextBlock
                                                        Text="{Binding Efficiency, StringFormat={}{0}%}"
                                                        FontSize="11" Foreground="{DynamicResource BrushTextSecondary}"/>
												</StackPanel>
												<StackPanel Orientation="Horizontal" Spacing="6">
													<TextBlock Text="Stress" FontSize="11" Foreground="{DynamicResource BrushTextSecondary}" Width="34"/>
													<ProgressBar
                                                        Value="{Binding StressLevel}"
                                                        Maximum="100"
                                                        Height="5" Width="80"
                                                        Foreground="{DynamicResource BrushDanger}"
                                                        VerticalAlignment="Center"/>
													<TextBlock
                                                        Text="{Binding StressLevel, StringFormat={}{0}%}"
                                                        FontSize="11" Foreground="{DynamicResource BrushTextSecondary}"/>
												</StackPanel>
												<TextBlock
                                                    FontSize="11" Foreground="{DynamicResource BrushTextSecondary}"
                                                    Text="{Binding StaffCount, StringFormat='👤 {0} staff'}"
                                                    VerticalAlignment="Center"/>
											</StackPanel>
										</StackPanel>

										<!-- Budget amount -->
										<TextBlock
                                            Grid.Column="2"
                                            Text="{Binding Budget, StringFormat='${0:N0}'}"
                                            FontSize="20"
                                            FontWeight="Bold"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Right"
                                            Margin="0,0,16,0"/>

										<!-- +/- buttons -->
										<StackPanel
                                            Grid.Column="3"
                                            Orientation="Horizontal"
                                            Spacing="6"
                                            VerticalAlignment="Center">
											<Button
                                                Width="36" Height="36"
                                                Background="{DynamicResource BrushBorderStrong}"
                                                CornerRadius="6"
                                                FontSize="18"
                                                HorizontalContentAlignment="Center"
                                                Command="{Binding $parent[ItemsControl].DataContext.DecreaseCommand}"
                                                CommandParameter="{Binding}"
                                                Content="−"/>
											<Button
                                                Width="36" Height="36"
                                                Background="{DynamicResource BrushBorderStrong}"
                                                CornerRadius="6"
                                                FontSize="18"
                                                HorizontalContentAlignment="Center"
                                                Command="{Binding $parent[ItemsControl].DataContext.IncreaseCommand}"
                                                CommandParameter="{Binding}"
                                                Content="+"/>
										</StackPanel>

									</Grid>
								</Border>
							</DataTemplate>
						</ItemsControl.ItemTemplate>
					</ItemsControl>

				</StackPanel>
			</Border>

		</StackPanel>
	</ScrollViewer>

</UserControl>