<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Class="Headquartz.App.ProductionMaintenanceView">

	<ScrollViewer>
		<StackPanel Margin="30" Spacing="20">

			<TextBlock FontSize="34" FontWeight="Bold" Text="Maintenance"/>

			<!-- CRITICAL BANNER -->
			<Border IsVisible="{Binding IsCritical}" Background="#7C2D12" CornerRadius="12" Padding="20">
				<StackPanel Orientation="Horizontal" Spacing="12">
					<TextBlock Text="🔧" FontSize="22" VerticalAlignment="Center"/>
					<StackPanel>
						<TextBlock Text="CRITICAL STRESS — IMMEDIATE MAINTENANCE REQUIRED"
                            FontSize="15" FontWeight="Bold" Foreground="#FDBA74"/>
						<TextBlock
                            Text="{Binding DeptStress, StringFormat='Stress at {0}%. Production efficiency severely degraded.'}"
                            Foreground="#FDBA74"/>
					</StackPanel>
				</StackPanel>
			</Border>

			<!-- KPI ROW -->
			<ItemsControl ItemsSource="{Binding Kpis}">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<WrapPanel/>
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>
				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<Border Width="200" Margin="10" Padding="20" Background="#202633" CornerRadius="12">
							<StackPanel>
								<TextBlock FontSize="13" Foreground="#9CA3AF" Text="{Binding Title}"/>
								<TextBlock FontSize="26" FontWeight="Bold" Text="{Binding Value}"/>
							</StackPanel>
						</Border>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>

			<!-- HEALTH BARS -->
			<Border Padding="20" Background="#202633" CornerRadius="12">
				<StackPanel Spacing="12">
					<TextBlock FontSize="18" FontWeight="Bold" Text="Production Line Health"/>
					<StackPanel Orientation="Horizontal" Spacing="10">
						<TextBlock Text="Efficiency" FontSize="12" Foreground="#9CA3AF" Width="80" VerticalAlignment="Center"/>
						<ProgressBar Value="{Binding DeptEfficiency}" Maximum="100" Height="12" Width="300"
                            Foreground="#10B981" VerticalAlignment="Center"/>
						<TextBlock Text="{Binding DeptEfficiency, StringFormat='{}{0}%'}"
                            FontSize="12" Foreground="#9CA3AF" VerticalAlignment="Center"/>
					</StackPanel>
					<StackPanel Orientation="Horizontal" Spacing="10">
						<TextBlock Text="Stress" FontSize="12" Foreground="#9CA3AF" Width="80" VerticalAlignment="Center"/>
						<ProgressBar Value="{Binding DeptStress}" Maximum="100" Height="12" Width="300"
                            Foreground="#EF4444" VerticalAlignment="Center"/>
						<TextBlock Text="{Binding DeptStress, StringFormat='{}{0}%'}"
                            FontSize="12" Foreground="#9CA3AF" VerticalAlignment="Center"/>
					</StackPanel>
				</StackPanel>
			</Border>

			<!-- MAINTENANCE OPTIONS -->
			<Border Padding="24" Background="#202633" CornerRadius="12">
				<StackPanel Spacing="16">
					<TextBlock FontSize="20" FontWeight="Bold" Text="Maintenance Options"/>

					<Grid ColumnDefinitions="*,20,*,20,*">

						<!-- Light Maintenance -->
						<Border Grid.Column="0" Padding="20" Background="#2B3242" CornerRadius="10">
							<StackPanel Spacing="12">
								<TextBlock Text="🔩 Light" FontSize="18" FontWeight="Bold"/>
								<TextBlock Text="Quick tune-up. Best for regular upkeep."
                                    FontSize="12" Foreground="#9CA3AF" TextWrapping="Wrap"/>
								<StackPanel Spacing="4">
									<TextBlock Text="− 15% Stress" FontSize="12" Foreground="#10B981"/>
									<TextBlock Text="+ 3% Efficiency" FontSize="12" Foreground="#10B981"/>
									<TextBlock Text="Cost: $1,000" FontSize="12" Foreground="#F97316"/>
								</StackPanel>
								<Button
                                    HorizontalAlignment="Stretch"
                                    Padding="12,9"
                                    Background="#374151"
                                    CornerRadius="8"
                                    Command="{Binding LightMaintenanceCommand}"
                                    Content="Schedule"/>
							</StackPanel>
						</Border>

						<!-- Emergency Repair -->
						<Border Grid.Column="2" Padding="20" Background="#2B3242" CornerRadius="10">
							<StackPanel Spacing="12">
								<TextBlock Text="⚡ Emergency" FontSize="18" FontWeight="Bold"/>
								<TextBlock Text="Rapid repair for active failures."
                                    FontSize="12" Foreground="#9CA3AF" TextWrapping="Wrap"/>
								<StackPanel Spacing="4">
									<TextBlock Text="− 25% Stress" FontSize="12" Foreground="#10B981"/>
									<TextBlock Text="+ 5% Efficiency" FontSize="12" Foreground="#10B981"/>
									<TextBlock Text="Resolves 1 event" FontSize="12" Foreground="#3B82F6"/>
									<TextBlock Text="Cost: $2,500" FontSize="12" Foreground="#F97316"/>
								</StackPanel>
								<Button
                                    HorizontalAlignment="Stretch"
                                    Padding="12,9"
                                    Background="#374151"
                                    CornerRadius="8"
                                    Command="{Binding EmergencyRepairCommand}"
                                    Content="Schedule"/>
							</StackPanel>
						</Border>

						<!-- Full Overhaul -->
						<Border Grid.Column="4" Padding="20" Background="#2B3242" CornerRadius="10">
							<StackPanel Spacing="12">
								<TextBlock Text="🏭 Full Overhaul" FontSize="18" FontWeight="Bold"/>
								<TextBlock Text="Complete line overhaul. Takes 4 ticks."
                                    FontSize="12" Foreground="#9CA3AF" TextWrapping="Wrap"/>
								<StackPanel Spacing="4">
									<TextBlock Text="− 40% Stress" FontSize="12" Foreground="#10B981"/>
									<TextBlock Text="+ 10% Efficiency" FontSize="12" Foreground="#10B981"/>
									<TextBlock Text="Cost: $5,000" FontSize="12" Foreground="#F97316"/>
								</StackPanel>
								<Button
                                    HorizontalAlignment="Stretch"
                                    Padding="12,9"
                                    Background="#374151"
                                    CornerRadius="8"
                                    Command="{Binding FullOverhaulCommand}"
                                    Content="Schedule"/>
							</StackPanel>
						</Border>

					</Grid>

					<Border
                        IsVisible="{Binding StatusMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                        Padding="14,10" Background="#2B3242" CornerRadius="8">
						<TextBlock Text="{Binding StatusMessage}" FontSize="13" TextWrapping="Wrap"/>
					</Border>
				</StackPanel>
			</Border>

			<!-- MAINTENANCE LOG -->
			<Border Padding="20" Background="#202633" CornerRadius="12">
				<StackPanel>
					<TextBlock FontSize="20" FontWeight="Bold" Text="Maintenance Log" Margin="0,0,0,12"/>
					<TextBlock IsVisible="{Binding !Log.Count}" Text="No maintenance performed yet." Foreground="#6B7280"/>
					<ItemsControl ItemsSource="{Binding Log}">
						<ItemsControl.ItemTemplate>
							<DataTemplate>
								<Border Margin="0,4" Padding="14,10" Background="#2B3242" CornerRadius="8">
									<Grid ColumnDefinitions="Auto,*,120">
										<Border
                                            Grid.Column="0"
                                            Padding="8,3"
                                            CornerRadius="5"
                                            Background="#374151"
                                            Margin="0,0,12,0"
                                            VerticalAlignment="Center">
											<TextBlock Text="{Binding Type}" FontSize="11" FontWeight="Bold" Foreground="#9CA3AF"/>
										</Border>
										<TextBlock Grid.Column="1" FontSize="13" Text="{Binding Description}" VerticalAlignment="Center"/>
										<TextBlock Grid.Column="2" FontSize="11" Foreground="#6B7280"
                                            Text="{Binding Timestamp}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
									</Grid>
								</Border>
							</DataTemplate>
						</ItemsControl.ItemTemplate>
					</ItemsControl>
				</StackPanel>
			</Border>

		</StackPanel>
	</ScrollViewer>
</UserControl>