<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Class="Headquartz.App.ProfileCreationView"
             Background="{DynamicResource BrushBgBase}">

	<Grid ColumnDefinitions="*,1080,*" RowDefinitions="*,Auto,*">

		<!-- ── Ambient glow ────────────────────────────────── -->
		<Canvas Grid.ColumnSpan="3" Grid.RowSpan="3" IsHitTestVisible="False">
			<Ellipse Canvas.Left="-100" Canvas.Top="-100"
                Width="500" Height="500" Opacity="0.06">
				<Ellipse.Fill>
					<RadialGradientBrush>
						<GradientStop Color="{DynamicResource ColorAccentPrimary}" Offset="0"/>
						<GradientStop Color="Transparent" Offset="1"/>
					</RadialGradientBrush>
				</Ellipse.Fill>
			</Ellipse>
		</Canvas>

		<!-- ── Center content ────────────────────────────────── -->
		<StackPanel Grid.Column="1" Grid.Row="1" Spacing="32">

			<!-- Header -->
			<StackPanel HorizontalAlignment="Center" Spacing="6" Margin="0,0,0,0">
				<TextBlock
                    Text="Create Your Profile"
                    FontSize="32"
                    FontWeight="Bold"
                    Foreground="{DynamicResource BrushTextPrimary}"
                    HorizontalAlignment="Center"/>
				<TextBlock
                    Text="This is how other players will see you"
                    FontSize="14"
                    Foreground="{DynamicResource BrushTextMuted}"
                    HorizontalAlignment="Center"/>
			</StackPanel>

			<!-- Two-column layout: Preview + Form -->
			<Grid ColumnDefinitions="200, 32, *">

				<!-- Profile card preview (left column) -->
				<Border
                    Grid.Column="0"
                    Padding="24"
                    Background="{DynamicResource BrushBgSurface}"
                    BorderBrush="{DynamicResource BrushBgInput}"
                    BorderThickness="1"
                    CornerRadius="16"
                    VerticalAlignment="Center">

					<StackPanel Spacing="16" HorizontalAlignment="Center">

						<!-- Avatar large -->
						<Border
                            Width="100" Height="100"
                            CornerRadius="50"
                            Background="{DynamicResource BrushBgInput}"
                            HorizontalAlignment="Center">
							<TextBlock
                                Text="{Binding SelectedAvatar}"
                                FontSize="56"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
						</Border>

						<!-- Username preview -->
						<TextBlock
                            Text="{Binding PreviewUsername}"
                            FontSize="20"
                            FontWeight="Bold"
                            Foreground="{DynamicResource BrushTextPrimary}"
                            HorizontalAlignment="Center"/>

						<!-- Level badge -->
						<Border
                            Padding="14,6"
                            CornerRadius="20"
                            Background="{DynamicResource BrushBgInput}"
                            HorizontalAlignment="Center">
							<TextBlock
                                Text="{Binding PreviewLevel}"
                                FontSize="12"
                                Foreground="{DynamicResource BrushTextSecondary}"/>
						</Border>

					</StackPanel>
				</Border>

				<!-- Spacer -->
				<Grid Grid.Column="1"/>

				<!-- Form (right column) -->
				<Border
                    Grid.Column="2"
                    Padding="28"
                    Background="{DynamicResource BrushBgSurface}"
                    BorderBrush="{DynamicResource BrushBgInput}"
                    BorderThickness="1"
                    CornerRadius="16">

					<StackPanel Spacing="24">

						<!-- Username field -->
						<StackPanel Spacing="8">
							<TextBlock
                                Text="USERNAME"
                                FontSize="11"
                                FontWeight="Bold"
                                Foreground="{DynamicResource BrushTextMuted}"
                                LetterSpacing="2"/>
							<TextBox
                                Text="{Binding Username}"
                                PlaceholderText="Enter your username..."
                                FontSize="15"
                                Padding="14,12"
                                Background="{DynamicResource BrushBgInput}"
                                BorderBrush="{DynamicResource BrushBgInput}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Foreground="{DynamicResource BrushTextPrimary}"/>
						</StackPanel>

						<!-- Avatar picker -->
						<StackPanel Spacing="8">
							<TextBlock
                                Text="CHOOSE AVATAR"
                                FontSize="11"
                                FontWeight="Bold"
                                Foreground="{DynamicResource BrushTextMuted}"
                                LetterSpacing="2"/>

							<ItemsControl ItemsSource="{Binding Avatars}">
								<ItemsControl.ItemsPanel>
									<ItemsPanelTemplate>
										<WrapPanel/>
									</ItemsPanelTemplate>
								</ItemsControl.ItemsPanel>
								<ItemsControl.ItemTemplate>
									<DataTemplate>
										<Button
                                            Width="48" Height="48"
                                            Margin="4"
                                            Padding="0"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Command="{Binding $parent[ItemsControl].DataContext.SelectAvatarCommand}"
                                            CommandParameter="{Binding}">
											<Border
                                                Width="44" Height="44"
                                                CornerRadius="10"
                                                Background="{DynamicResource BrushBgBase}"
                                                BorderBrush="{DynamicResource BrushAccentPrimary}"
                                                BorderThickness="2">
												<TextBlock
                                                    Text="{Binding Emoji}"
                                                    FontSize="24"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
											</Border>
										</Button>
									</DataTemplate>
								</ItemsControl.ItemTemplate>
							</ItemsControl>

						</StackPanel>

						<!-- Error message -->
						<Border
                            IsVisible="{Binding HasError}"
                            Padding="12,10"
                            Background="{DynamicResource BrushBadgeDangerBg}"
                            CornerRadius="8"
                            BorderBrush="{DynamicResource BrushAlertCriticalBg}"
                            BorderThickness="1">
							<TextBlock
                                Text="{Binding ErrorMessage}"
                                FontSize="13"
                                Foreground="{DynamicResource BrushBadgeDangerFg}"
                                TextWrapping="Wrap"/>
						</Border>

						<!-- Continue button -->
						<Button
                            HorizontalAlignment="Stretch"
                            Padding="0,14"
                            CornerRadius="10"
                            Command="{Binding CreateProfileCommand}">
							<Button.Background>
								<LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,0%">
									<GradientStop Color="{DynamicResource ColorAccentPrimary}" Offset="0"/>
									<GradientStop Color="{DynamicResource ColorAccentPrimaryHover}" Offset="1"/>
								</LinearGradientBrush>
							</Button.Background>
							<TextBlock
                                Text="Continue →"
                                FontSize="15"
                                FontWeight="Bold"
                                Foreground="{DynamicResource BrushTextPrimary}"
                                HorizontalAlignment="Center"/>
						</Button>

					</StackPanel>
				</Border>

			</Grid>

		</StackPanel>

	</Grid>

</UserControl>